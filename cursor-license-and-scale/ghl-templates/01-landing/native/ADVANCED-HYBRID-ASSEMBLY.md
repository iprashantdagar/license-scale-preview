# Landing — Advanced hybrid assembly

## Phase 1 (now): Full-body fidelity
Import `03-FULL-BODY-one-block.html` as one Custom Code block.

Optional CRM-ready: use `03b-FULL-BODY-with-ghl-form-slot.html` and drop a GHL Form into the dashed survey slot (or keep Typeform temporarily).

## Phase 2: Peel native (recommended order)
Build top→bottom as separate GHL rows:

1. **Native** Nav row (logo, links, Apply button)
2. **Native** Hero copy row (H1, sub, CTA) + **Custom Code** for Wistia + stats (`sections/01-hero.html` trimmed)
3. **Custom Code** Case studies (`02-case-studies.html`)
4. **Native** Testimonial + process (`03` copy from COPY-SHEET)
5. **Custom Code** Press / Outcomes / Community / Playbooks
6. **Native** Jobs teaser + FAQ
7. **Native GHL Form** Survey
8. **Native** Footer

Keep Header/Footer tracking CSS+JS until Custom Code sections are gone.

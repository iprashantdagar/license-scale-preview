# Local GHL preview

Open `index.html` via any static server to QA templates before importing into GHL.

These files are assembled from the paste kits for convenience — **import into GHL from the parent `0X-*/` folders**, not from here.
